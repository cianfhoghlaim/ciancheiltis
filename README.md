# Ciancheiltis

Pure Irish-language datasets + non-educational Celtic-language pipelines.

Carved from `cianfhoghlaim` per the v2 plan §A (bilingual educational carve rule)
+ parent change `2026-08-24-dlt-sources-to-multi-repo-scaffold-v1` §21.3.

Run `mise run ciancheiltis:smoke-all` (or `uv run pytest tests/dlt/test_imports.py -q`).
