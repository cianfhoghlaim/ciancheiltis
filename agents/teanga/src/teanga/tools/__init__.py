"""Teanga tools (Celtic language agent tool surface).

Status: STUB. The real tool implementations back onto the canonical
language DLT sources (`dlt_sources/language/`) and BAML extraction
contracts (`baml_src/processing/gemini_deep_research.baml`). They ship
with the umbrella change `2026-09-06-ciancheiltis-v1` Phase 2.

Each stub returns a deterministic placeholder so the agents can load
and run their keyword-classification flow against real questions.
"""
